import { motion } from 'framer-motion';

const GlowOrb = ({
  className = '',
  style = {},
  color = 'gold',
  size = 'lg',
  intensity = 'medium',
  animated = true,
  blur = '3xl',
  ...props
}) => {
  return null;
};

export default GlowOrb;
